http:
  routers:
    SERVICE-https:
      rule: "Host(`example.com`)"
      entryPoints:
        - websecure
      service: SERVICE-https
      tls:
        certResolver: cloudflare

  services:
    SERVICE-https:
      loadBalancer:
        servers:
          - url: "http://192.168.1.100:8080"